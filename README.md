### How to run locally
 - Open two terminals
 - In the first:
   - `cd frontend`
   - `npm install`
   - `npm run dev`
&nbsp;
 - In the second:
   - `cd backend`
   - `npm install`
   - Create a .env file with MONGO_URI
   - `nodemon index.js`